# Normalize line endings. Force LF for files executed/parsed on the Linux VPS so a
# Windows checkout (autocrlf) can't introduce CRLF that breaks them.
* text=auto

*.sh        text eol=lf
*.service   text eol=lf
Caddyfile   text eol=lf
*.py        text eol=lf
